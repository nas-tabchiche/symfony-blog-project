{% extends "base_admin.html.twig" %}


{% block content %}
	<div class="liste_posts">
		<div class="container">
			<div class="row">
				<div class="col s12">
					<h5>Commentaires</h5>
					<table>
						<tr>
							<th>Post n°</th>
							<th>Nom d'utilisateur</th>
							<th>Contenu</th>
							<th>Actions</th>
						</tr>
						{% set i = 1 %}
						{% for comment in comments %}
							<tr>
								<td>{{ comment.post }}</td>
								<td>
									{{ comment.username }}
								</td>
								<td>
									{{ comment.content }}
								</td>
								<td>
									<a href="#">
										<i class="material-icons blue-text">valider</i>
									</a>
									<a href="{{ path('delete_comm', {'id': comment.id}) }}">
										<i class="material-icons red-text">supprimer</i>
									</a>
								</td>
							</tr>
							{% set i = i+1 %}
						{% endfor %}
					</table>
				</div>
			</div>
		</div>
	{% endblock %}
